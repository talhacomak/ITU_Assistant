<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.myapplication">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".add_drop_classes">

        </activity>
        <activity android:name=".add_class_page">

        </activity>
        <activity android:name=".classAttributes">

        </activity>
        <activity android:name=".attendance">

        </activity>
        <activity android:name=".add_note">

        </activity>
        <activity android:name=".settings"/>
        <activity android:name=".add_note_page"/>
        <activity android:name=".add_tasks"/>
        <activity android:name=".calender_tasks"/>
    </application>

    <dist:module dist:instant="true" />

</manifest>